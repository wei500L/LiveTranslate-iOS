# 更新日志

## 2026-09-07（第二十二轮 · 续：服务端模型托管与下载源选择）

- **模型下载源**：翻译模型管理新增「模型下载源」——平台直连（Hugging Face，默认）或**云端服务器**（自建同步服务器预下载后供客户端拉取）。
- **服务器就绪探测**：选云端服务器时，管理页实时显示每台模型在服务器上的就绪状态（GET /v1/models/ai，Bearer 认证）；未登录/未配置给出明确提示，绝不静默回退另一来源。
- **一致性**：两种来源下载同一字节，客户端一律按自带 manifest 做 SHA256 + 大小校验；服务器下载走 Range 断点续传与既有暂停/继续体系。
- **配套服务端（Go 仓库）**：`MODEL_STORAGE_DIR` 配置 + `livetranslate-server download-models` 一次性预下载命令（固定 revision、SHA256 门禁、断点续传、幂等）+ `/v1/models/ai` 目录与文件路由（RequireUser 认证）；compose/.env.example/README/ENVIRONMENTS 文档化。

## 2026-09-07（第二十二轮）

新增移动端离线翻译模型支持：默认离线翻译（Hy-MT2）、可选省电翻译（MiLMMT）、独立图片理解模型（Gemma 4 E2B）与 Apple 系统翻译兜底。

- **离线翻译（默认）**：Hy-MT2-1.8B Q4_K_M（约 1.13 GB，Apache-2.0）经 llama.cpp 在本机完成俄语→简体中文翻译，完全断网可用；断网时课堂不再进入离线暂停态。新用户默认启用；已配置云端 API 的老用户保持云端不变。
- **省电模式**：MiLMMT-46-1B-v1.0 Q4_K_M（约 806 MB，gemma 许可）作为设置中的可选翻译方式。
- **图片理解**：Gemma 4 E2B it（约 2.59 GB，Apache-2.0，LiteRT-LM 运行时）下载后自动接管课堂照片分析与视觉问答（同协议本地实现）；仅在请求期间加载、用完即释放，绝不与翻译模型长期共存。
- **系统翻译兜底**：本地模型加载/推理失败时单请求降级 Apple Translation（iOS 26+ 可编程会话；更早系统诚实报错，不静默）。
- **模型管理**：三台模型全部走既有"清单 + 实测 SHA256 + 断点续传 + 暂停/继续/删除/重新校验"分发体系（新「翻译模型管理」页）；下载前检查磁盘空间；模型文件不进 Git、不打入初始 App 包。
- **会话安全**：课堂会话把所选翻译模型锁定到会话结束，结束后才允许切换/删除；结束流程先排干在途翻译再解锁；俄语原文始终先于翻译落库，任何失败不丢转录。
- **切换语义**：同一时刻最多一台翻译模型驻留，切换即完整卸载旧模型；课堂进行中的历史记录不受切换影响。
- **运行时**：llama.cpp（固定 commit `465e49b9`，含 Hy-MT2 所需 STQ kernel，Metal 加速）与 LiteRT-LM（固定 commit）均由脚本获取/构建、不入库；CI 已接入两框架的获取步骤。
- **测试**：新增单元测试（提示词逐字节契约、manifest 解码回归、提供方默认值/迁移、兜底链、会话锁定）与模型相关集成测试（真实俄语→中文推理、双模型加载/切换/释放、缺模型语义，模型缺失时自动跳过）。
- **实测结果**：模拟器上 Hy-MT2 真实推理俄→中（「Сегодня мы изучаем производные сложных функций.」→「今天我们学习复合函数的导数。」，5.5s 含加载）与 MiLMMT（1.6s）通过；与官方 llama.cpp CLI 用同一提示词交叉验证一致；全部既有单元/集成测试通过（GoServerE2E 一次环境性抖动重跑通过）。模拟器限制：llama.cpp Metal 在模拟器产生全零 logits（强制 CPU 规避，真机不受影响）；Gemma 的 LiteRT 运行时模拟器切片为不可用 stub——**图片理解需真机验证**。
- 文档：新增 `docs/OFFLINE_MODELS.md`；更新 README、ARCHITECTURE、PRIVACY、MODEL_DISTRIBUTION 与 THIRD_PARTY_NOTICES（许可明细，含 MiLMMT 的 gemma 条款人工复核提示）。

## 2026-09-07（第二十一轮）

俄语表单逐项填写与字段翻译助手：`form-draft.json` sidecar、字段询问联动、翻译两段式。iOS `efac1ef`，Go 零修改。

## 2026-09-06（第二十轮）

连续收听随身翻译：暂停门、记入事项 sheet、文件模板。iOS ci/round20-continuous-interpreter。
